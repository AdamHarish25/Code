/**
 * Duitly - Smart Budgeting App
 * Main entry point with onboarding flow
 */

import { OnboardingFlow } from "@/components/onboarding";

export default function Home() {
  return <OnboardingFlow />;
}
